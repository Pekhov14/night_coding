Ограничение доступа по ip с маской.
Передаю &$item
в переменную $item попадает каждый эл массива $pards_allowed (4 састи '125.0.0.5')
который есть разбитый $allowed_ips (каждый ip), который был одним массивом.

Специфика цикла foreach, он просто проходит по массиву и не меняет его эл.
Что бы изменить в цикле передаю по ссылке &.